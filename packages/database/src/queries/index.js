const { userQueries } = require("./user.queries");
const { secretQueries } = require("./secret.queries");
const { rotationLogQueries } = require("./rotation-log.queries");

module.exports = { userQueries, secretQueries, rotationLogQueries };
